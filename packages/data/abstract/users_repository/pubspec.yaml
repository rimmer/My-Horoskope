name: users_repository
description: users repository data abstraction
version: 0.0.1
author: bgoncharuck
publish_to: none

environment:
  sdk: ">=2.6.0 <3.0.0"

dependencies:
  meta: ^1.1.8
  json_annotation: ^3.0.1
  
  user_model:
    path: ../../../models/user_model  

dev_dependencies:
  build_runner: ^1.10.0
  json_serializable: ^3.2.0
