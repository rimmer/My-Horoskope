name: polls_repository
description: used to correctly save and load polls by user id
version: 0.0.1
author: bgoncharuck

environment:
  sdk: ">=2.6.0 <3.0.0"
  
dependencies:
  poll_model:
    path: ../../../models/poll_model  
  userpoll:
    path: ../userpoll
