name: prophecy_to_show_storage_flutter
description: correctly save and load what prophecies to show on flutter platform
version: 0.0.1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  storage_access: ^0.0.6
  
  prophecy_to_show:
    path: ../../../models/prophecy_to_show
  prophecy_to_show_storage:
    path: ../../abstract/prophecy_to_show_storage  

dev_dependencies:
flutter:
