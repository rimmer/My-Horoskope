name: polls_repository_flutter
description: correctly save and load polls by user id on flutter platform
version: 0.0.1
author:
homepage:

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  storage_access: ^0.0.2
  
  int_datetime:
    path: ../../../int_datetime

  poll_model:
    path: ../../../models/poll_model  
  userpoll:
    path: ../../abstract/userpoll
  polls_repository:
    path: ../../abstract/polls_repository
  
  json_annotation: ^3.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^1.10.0
  json_serializable: ^3.2.0

flutter:
