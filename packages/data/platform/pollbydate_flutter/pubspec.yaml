name: pollbydate_flutter
description: pollbydate for correctly saving-loading map of user polls by date with flutter using storage_access
version: 0.0.1
author:
homepage:

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  storage_access: ^0.0.2

  # I don't want to publish this package, it is too small and uncompleted.
  int_datetime:
    path: ../../../int_datetime

  poll_model:
    path: ../../../models/poll_model  
  userpoll:
    path: ../../abstract/userpoll
  pollbydate:
    path: ../../abstract/pollbydate

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
