name: algorithm
description: separate module for calculations
author: bgoncharuck
version: 0.0.1+1
publish_to: none

environment:
  sdk: ">=2.6.0 <3.0.0"

dependencies:
  meta: ^1.1.8
  
  base:
    path: ../base

  flutter:
    sdk: flutter

dev_dependencies:
  test: ^1.16.8

flutter:

