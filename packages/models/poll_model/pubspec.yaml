name: poll_model
description: poll_model saves user choise in poll
author: bgoncharuck
version: 0.0.1+1
publish_to: none

environment:
  sdk: ">=2.6.0 <3.0.0"

dependencies:
  meta: ^1.1.8
  json_annotation: ^3.0.1
  
dev_dependencies:
  build_runner: ^1.10.0
  json_serializable: ^3.3.0
